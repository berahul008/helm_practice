apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-test
  name: my-test
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: my-test
  type: NodePort
status:
  loadBalancer: {}
